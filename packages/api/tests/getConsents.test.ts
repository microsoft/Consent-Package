// This test verifies the business logic of the getConsents function
describe("getConsents function", () => {
  it("should return an array of consents", () => {
    expect(true).toBe(true);
  });
});
