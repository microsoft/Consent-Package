name: Build and Test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup
      - name: Clean
        run: pnpm clean
      - name: Build
        run: pnpm build
      - name: Run tests
        run: pnpm test
